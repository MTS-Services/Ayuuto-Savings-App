import 'package:flutter/material.dart';

class TotalGroupScreen extends StatelessWidget {
  const TotalGroupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}